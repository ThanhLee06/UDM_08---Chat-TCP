# Copy to smtp.properties on the server only. Never commit real credentials.
smtp.host=smtp.gmail.com
smtp.port=587
smtp.username=your-demo-mailbox@example.com
smtp.password=replace-with-your-provider-app-password
